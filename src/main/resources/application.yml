spring:
  jpa:
    show-sql: false
    properties:
      hibernate.format_sql: false
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB
  application:
    name: Yard_Sale_User
  h2:
    console:
      path: /h2
      enabled: true
  profiles:
    active: dev
server:
  port: 8080
logging:
  level:
    org.springframework.security: debug